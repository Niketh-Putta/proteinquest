/**
 * DISABLED — do not use.
 *
 * This script generated low-quality SVG silhouette dragon art that was rejected.
 * Dragon assets live in assets/character/dragons/{fire,ice,forest}-1..5.png
 * and must match the original cinematic 3D render style.
 *
 * To add or replace art, edit PNGs directly or use high-fidelity image generation
 * referencing the existing good assets — never re-enable this approach.
 */
console.error(
  'generate-dragon-art.mjs is disabled. Edit assets/character/dragons/*.png directly.',
);
process.exit(1);
