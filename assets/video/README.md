# Onboarding demo video

- `onboarding-demo.mp4` is committed (used by `IntroPhone`).
- Optional HQ: `cp onboarding-reference/web/public/media/onboarding-demo-hq.mp4 assets/video/` then point `IntroPhone` at the HQ file.
